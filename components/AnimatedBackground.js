import React from 'react';
import '../styles/animatedBackground.css';

const AnimatedBackground = () => {
  return <div></div>; // No need for extra elements, background is handled by CSS
};

export default AnimatedBackground;
